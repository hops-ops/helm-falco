apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: falcos.helm.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: helm.hops.ops.com.ai/v1alpha1
    kind: Falco
  mode: Pipeline
  pipeline:
  - functionRef:
      name: hops-ops-helm-falcorender
    step: render
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
